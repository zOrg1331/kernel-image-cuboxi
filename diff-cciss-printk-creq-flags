--- ./drivers/block/cciss.c.orig	2008-07-10 16:46:27.000000000 +0400
+++ ./drivers/block/cciss.c	2008-07-10 16:46:38.000000000 +0400
@@ -2717,7 +2717,7 @@ static void do_cciss_request(request_que
 		c->Request.CDBLen = creq->cmd_len;
 		memcpy(c->Request.CDB, creq->cmd, BLK_MAX_CDB);
 	} else {
-		printk(KERN_WARNING "cciss%d: bad request type %d\n", h->ctlr, creq->cmd_type);
+		printk(KERN_WARNING "cciss%d: bad request type %d\n", h->ctlr, creq->flags);
 		BUG();
 	}
 
