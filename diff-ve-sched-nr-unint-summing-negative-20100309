diff --git a/kernel/sched.c b/kernel/sched.c
index 26f129d..a030c56 100644
--- a/kernel/sched.c
+++ b/kernel/sched.c
@@ -3287,6 +3287,8 @@ unsigned int nr_unint_vsched(struct vcpu_scheduler *vsched)
 
 	sum += (unsigned int)atomic_read(&vsched->nr_unint_fixup);
 #endif
+	if (unlikely((int)sum < 0))
+		sum = 0;
 	return sum;
 }
 
